name: backend
description: Backend do site de login do brechó, em Dart Frog
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  sqlite3: ^2.4.0
  bcrypt: ^1.1.3
  mime: ^1.0.4

dev_dependencies:
  test: ^1.24.0

